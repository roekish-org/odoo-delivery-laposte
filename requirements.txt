# Optional runtime dependencies, imported lazily and fail-closed if missing.
# roulier: Colissimo label generation.
# zeep:    Colissimo Point Retrait (pickup point) SOAP client.
# Delivengo needs nothing extra: it uses `requests`, shipped with Odoo.
roulier
zeep
